Setup the installation paths
--- a/config.mk
+++ b/config.mk
@@ -7,8 +7,8 @@
 BASH = /bin/bash
 
 # paths
-PREFIX = /usr/local
+PREFIX = /usr
 BINDIR = ${PREFIX}/bin
 LIBDIR = ${PREFIX}/lib
-SYSCONFDIR = ${PREFIX}/etc/${NAME}
+SYSCONFDIR = /etc/${NAME}
 DOCDIR = ${PREFIX}/share/doc/${NAME}
