Setup the installation paths
--- config.mk
+++ config.mk
@@ -15,8 +15,8 @@
 #HARDLINKER = pax -rwl
 
 # paths
-PREFIX = /usr/local
+PREFIX = /usr
 BINDIR = ${PREFIX}/bin
 LIBDIR = ${PREFIX}/lib
-SYSCONFDIR = ${PREFIX}/etc/${NAME}
+SYSCONFDIR = /etc/${NAME}
 DOCDIR = ${PREFIX}/share/doc/${NAME}
