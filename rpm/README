The rpm/ directory contains a spec file to create RPM files.

The `./build.sh` scripts will build the rpm package from this spec file. You can
override version by passing it as first argument: `./build.sh 2.0`. The rpm is
saved in usual rpmbuild location.
