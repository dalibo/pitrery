NAME = replitr
VERSION = 1.0

# Customize below to fit your system

# Bash is mandatory
BASH = /bin/bash

# paths
PREFIX = /usr/local
BINDIR = ${PREFIX}/bin
LIBDIR = ${PREFIX}/lib/${NAME}
SYSCONFDIR = ${PREFIX}/etc/${NAME}
