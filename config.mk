NAME = pitrery
VERSION = 1.2

# Customize below to fit your system

# Bash is mandatory
BASH = /bin/bash

# paths
PREFIX = /usr/local
BINDIR = ${PREFIX}/bin
LIBDIR = ${PREFIX}/lib
SYSCONFDIR = ${PREFIX}/etc/${NAME}
